meta {
  name: Confirm Appointment (Provider)
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/api/v1/appointments/{{appointmentId}}/confirm
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: Bearer {{token}}
}
